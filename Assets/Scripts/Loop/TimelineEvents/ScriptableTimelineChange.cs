using UnityEngine;

// Used to track what has happened during the loop
[CreateAssetMenu(fileName = "ScriptableTimelineChange", menuName = "Scriptable Objects/TimelineChange")]
public class ScriptableTimelineChange : ScriptableObject
{
    
}
